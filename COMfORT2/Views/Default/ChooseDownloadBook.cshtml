@model List<COMfORT2.Book>
@{
    ViewBag.WindowTitle = "AMOS: Export Books";
    Layout = "~/Views/Shared/_ManagementShellLayout.cshtml";
    Response.Cache.SetCacheability(HttpCacheability.NoCache);
    Response.Cache.SetNoStore();
}
@section Styles{
    <link rel="stylesheet" href="~/fonts/FiraSans/Fonts.css">
    <link rel="stylesheet" href="~/Content/css/Management/ChooseDownloadBook.css?v=0">
}

<div>
    <table style="margin: 20px auto">
        <tr>
            <th></th>
            <th>Title</th>
            <th>Version</th>
            <th>Created</th>
            <th>Modified</th>
        </tr>
        @foreach (var book in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink("download", "DownloadBook", new { id = book.BookId })
                </td>
                <td>@book.Name</td>
                <td>@book.Version</td>
                <td>@book.CreatedBy <br /> @book.CreateDate.ToShortDateString()</td>
                <td>@book.ModifiedBy <br /> @book.ModifyDate.ToShortDateString()</td>
            </tr>
        }
    </table>
</div>

@section Scripts{
    <script>
    </script>
    <script type="text/javascript" src="~/Content/js/Management/ChooseDownloadBook.js?v=0"></script>
}